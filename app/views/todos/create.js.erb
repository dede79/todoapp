const ul = document.querySelector("ul")
const li = document.createElement("li")

ul.insertBefore(li, ul.firstChild)
//outerHTML is the tag definition
li.outerHTML = "<%= j render "todo", {todo: @todo} %>"
