
<!-- remote true will make an ajax request -->
<%= form_for Todo.new, remote:true do |f| %>
 <%= f.text_field :title %> 
 <%= f.submit "Add" %>
<% end %>

<ul>
  <% @todos.each do |t| %>
   <%= render "todo", {todo: t} %>
  <%= render "edit", {todo: t} %>
 <% end %>
</ul>